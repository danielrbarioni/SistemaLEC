## 1. Frontend Form & Filter Updates

- [x] 1.1 In `frontend/src/views/InteracoesLec.vue`, add computation to resolve registered users with function `"Médico"` for the currently selected specialty.
- [x] 1.2 Update the "Médico Responsável" input field in `InteracoesLec.vue` to be disabled with placeholder `"Selecione a especialidade primeiro"` when no specialty is defined.
- [x] 1.3 Bind the datalist/options for "Médico Responsável" to the specialty-filtered doctor list.

## 2. Verification

- [x] 2.1 Verify input behavior when specialty is unset vs set, and verify filtered doctor options in form.
